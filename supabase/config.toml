project_id = "cxdknqvqipgxehlozzpi"

[api]
# Port to use for the API URL.
port = 54321
# Schemas to expose in your API. Tables, views and stored procedures in this schema will get API
# endpoints. public and storage are always included.
schemas = ["public", "storage"]
# Extra schemas to add to the search_path of every request. public is always included.
extra_search_path = ["public", "extensions"]
# Maximum rows returns in a single response. Defaults to 1000.
max_rows = 1000

[db]
# Port to use for the local database URL.
port = 54322
# Port used by db diff command to initialize the shadow database.
shadow_port = 54320
# The database major version to use. This has to be the same as your remote database's. Run `SHOW
# server_version` on the remote database to check.
major_version = 15

[studio]
enabled = true
# Port to use for Supabase Studio.
port = 54323
# External URL of the API server that frontend connects to.
api_url = "http://localhost"

[functions]
# Port to use for Functions.
# port = 54324 # Commenting out due to parse error
# Function to be served if path doesn't match any other function
# default_function_path = "" # Commenting out due to parse error

[functions.get_shelf_life_defaults]
verify_jwt = true

[inbucket]
# Port to use for the Inbucket API.
port = 54325
# Port to use for the SMTP port.
smtp_port = 54326
# Port to use for the POP3 port.
pop3_port = 54327

[storage]
# The maximum file size allowed (e.g. "5MB", "500KB").
file_size_limit = "50MiB"

[auth]
# The base URL of your website. Used as an allow-list for redirects and for constructing URLs used
# in emails.
site_url = "http://localhost:3000"
# A list of *exact* URLs that auth providers are permitted to redirect to post authentication.
additional_redirect_urls = ["https://localhost:3000"]
# How long tokens are valid for, in seconds. Defaults to 3600 (1 hour), maximum 604,800 (1 week).
jwt_expiry = 3600
# If disabled, the refresh token will never expire.
enable_refresh_token_rotation = true
# Allows refresh tokens to be reused after expiry, up to the specified interval in seconds.
# Requires enable_refresh_token_rotation = true.
refresh_token_reuse_interval = 10
# Allow/disallow new user signups to your project.
enable_signup = true
# enable_mfa = true # This setting is typically managed in the dashboard, commenting out.

# [auth.email_templates] # Commenting out as this seems to be a dashboard-only setting
# reset_password_subject = "Reset Your Password for Blue Horizon"
# reset_password_body = """
# Hi {{ .User.Email }},
# 
# We received a request to reset the password for your account on Blue Horizon. If you made this request, please click the link below to reset your password:
# 
# {{ .ConfirmationURL }}
# 
# If you did not request a password reset, you can safely ignore this email. Your account is still secure.
# 
# If you have any questions or need further assistance, feel free to contact us at support@bluehorizon.com.
# 
# Thank you for using Blue Horizon!
# 
# Best regards,  
# The Blue Horizon Team
# """

[analytics]
enabled = false
port = 54328
vector_port = 54329
